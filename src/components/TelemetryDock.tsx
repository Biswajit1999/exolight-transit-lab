import { useExoStore } from "../state/useExoStore";
export function TelemetryDock(){ const t=useExoStore(s=>s.selectedTarget); const fit=useExoStore(s=>s.fit); return <section className="panel"><h2>Physics Telemetry</h2><div className="metric-list"><div><span>Target</span><b>{t?.pl_name ?? "—"}</b></div><div><span>Host</span><b>{t?.hostname ?? "—"}</b></div><div><span>Rp/R★</span><b>{fit?.rpRs.toFixed(4) ?? "—"}</b></div><div><span>a/R★</span><b>{fit?.aRs.toFixed(2) ?? "—"}</b></div><div><span>e</span><b>{fit?.eccentricity.toFixed(3) ?? "—"}</b></div><div><span>ω</span><b>{fit?.omegaDeg.toFixed(1) ?? "—"}°</b></div></div></section>; }
