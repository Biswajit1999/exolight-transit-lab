import { useExoStore } from "../state/useExoStore";
export function ResidualDock(){ const r=useExoStore(s=>s.residualSummary); return <section className="panel"><h2>Residuals</h2><div className="metric-grid"><div><span>RMS</span><b>{r?`${r.rmsPpm.toFixed(0)} ppm`:"model-only"}</b></div><div><span>MAD</span><b>{r?`${r.madPpm.toFixed(0)} ppm`:"—"}</b></div><div><span>χ² proxy</span><b>{r?r.chi2Proxy.toFixed(2):"—"}</b></div><div><span>N</span><b>{r?r.n:"—"}</b></div></div><p className="microcopy">Load MAST/CSV photometry to activate observed-minus-model residuals.</p></section>; }
